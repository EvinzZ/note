# yum安装jdk

1.选择yum安装jdk-1.8，不用手动配置环境变量，`Java_home=/usr`

                ```bash
yum -y install java-1.8*              
                ```

2.查看JDK版本

              ```bash
java -version  
              ```

